<h1>Tshirt creation</h1>

<form action="{{ path('admin_tshirt_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_tshirt') }}">
            Back to the list
        </a>
    </li>
</ul>
