<h1>Size creation</h1>

<form action="{{ path('admin_size_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_size') }}">
            Back to the list
        </a>
    </li>
</ul>
